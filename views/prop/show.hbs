<div class="portrait-gallery">
    <img src="{{ prop.source }}" alt="Props for {{ prop.title }}">
</div>
<div class="description-container">
    <div class="text-container">
        <p>{{ prop.description }}</br><span>{{#if prop.year}}{{prop.title}},{{/if}} </span> {{ prop.year }}</p>
        <p class="credits">{{ prop.creditsOne }}</p>
        <p class="credits">{{ prop.creditsTwo }}</p>
    </div>
</div>